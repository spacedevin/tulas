import { generateStream } from "tish:tulas"

let modelPath = "./models/Llama-3.2-1B-Instruct-8bit"
let prompt = "Tell me about London in three sentences."
let limit = 64.0

let acc = ""
generateStream(modelPath, prompt, limit, (chunk) => {
  acc = acc + chunk
})
console.log(acc)
